import { ObjectValue } from "./base.js";
export class FFIObject extends ObjectValue {
  name;
  code;
  runtime;
  declaredIn;
  options;
  static friendlyName = '번역';
  paramNames;
  constructor(name, code, runtime, declaredIn, options = {}){
    super(), this.name = name, this.code = code, this.runtime = runtime, this.declaredIn = declaredIn, this.options = options, this.paramNames = [];
  }
  async run(args, callerScope) {
    const result = await this.declaredIn.session.runFFI(this.runtime, this.code, args, callerScope);
    return result;
  }
}
//# sourceMappingURL=ffi.js.map