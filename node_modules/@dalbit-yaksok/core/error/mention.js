import { blue, bold, YaksokError } from "./common.js";
export class ErrorInModuleError extends YaksokError {
  constructor(props){
    super(props);
    this.child = props.child;
    this.message = `다른 약속 파일 ${blue(bold(props.resource.fileName))}에서 오류가 발생했어요.`;
  }
}
//# sourceMappingURL=mention.js.map