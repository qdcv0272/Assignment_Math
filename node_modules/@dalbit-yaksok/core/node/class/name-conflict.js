import { NotDefinedIdentifierError } from "../../error/variable.js";
function resolveExistingIdentifierValue(scope, name) {
  try {
    return scope.getVariable(name);
  } catch (error) {
    if (error instanceof NotDefinedIdentifierError) {
      return undefined;
    }
    throw error;
  }
}
function resolveExistingFunctionObject(scope, name) {
  try {
    return scope.getFunctionObject(name);
  } catch (error) {
    if (error instanceof NotDefinedIdentifierError) {
      return undefined;
    }
    throw error;
  }
}
export function hasExistingClassNameConflict(scope, name) {
  return resolveExistingIdentifierValue(scope, name) !== undefined || resolveExistingFunctionObject(scope, name) !== undefined;
}
//# sourceMappingURL=name-conflict.js.map