import { functionHeaderToPattern } from "./common.js";
import { DeclareEvent } from "../../../../../node/event.js";
import { Evaluable, Expression, Identifier } from "../../../../../node/base.js";
const PREFIX = [
  {
    type: Identifier,
    value: '이벤트'
  },
  {
    type: Expression,
    value: '('
  },
  {
    type: Evaluable
  },
  {
    type: Expression,
    value: ')'
  },
  {
    type: Expression,
    value: ','
  }
];
export function tokensToEventDeclareRule(headerTokens) {
  const pattern = [
    ...PREFIX,
    ...functionHeaderToPattern(headerTokens.slice(6))
  ];
  return {
    pattern,
    factory: (nodes, matchedTokens)=>{
      const eventId = nodes[2].value;
      const name = headerTokens.map((token)=>token.value).join('').trim();
      return new DeclareEvent({
        name,
        eventId
      }, matchedTokens);
    }
  };
}
//# sourceMappingURL=event-declare-rule.js.map