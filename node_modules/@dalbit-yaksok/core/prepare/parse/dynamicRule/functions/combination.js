export function getCombination(arr) {
  const result = [];
  function dfs(index, current) {
    if (index === arr.length) {
      result.push(current);
      return;
    }
    for (const item of arr[index]){
      dfs(index + 1, [
        ...current,
        item
      ]);
    }
  }
  dfs(0, []);
  return result;
}
//# sourceMappingURL=combination.js.map