import { FFIResultTypeIsNotForYaksokError } from "../error/ffi.js";
import { ValueType } from "../value/base.js";
export function assertValidReturnValue(node, value) {
  if (value instanceof ValueType) {
    return;
  }
  const ffiName = node.value;
  throw new FFIResultTypeIsNotForYaksokError({
    ffiName,
    value,
    tokens: node.tokens
  });
}
//# sourceMappingURL=assert-valid-return-value.js.map