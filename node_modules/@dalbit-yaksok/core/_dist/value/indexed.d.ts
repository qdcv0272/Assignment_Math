import { ObjectValue, ValueType } from "./base.js";
import type { ListValue } from "./list.js";
export declare class IndexedValue extends ObjectValue {
  protected entries: Map<string | number, ValueType>;
  static friendlyName: string;
  constructor(entries: Map<string | number, ValueType>);
  getItem(index: string | number): ValueType;
  setItem(index: string | number, value: ValueType): void;
  getEntries(): IterableIterator<[string | number, ValueType]>;
  getItemsFromKeys(keysListValue: ListValue): IndexedValue;
  override toPrint(): string;
  public enumerate(): Iterable<ValueType>;
}
//# sourceMappingURL=indexed.d.ts.map