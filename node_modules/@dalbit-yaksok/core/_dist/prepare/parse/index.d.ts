import { Block } from "../../node/block.js";
import type { CodeFile } from "../../type/code-file.js";
import type { Rule } from "./type.js";
/**
 * 파싱 결과를 담는 객체입니다.
 */ interface ParseResult {
  ast: Block;
  exportedRules: Rule[];
}
/**
 * `CodeFile`에 포함된 토큰들을 파싱하여 추상 구문 트리(AST)를 생성합니다.
 */ export declare function parse(codeFile: CodeFile, optimistic?: boolean): ParseResult;
//# sourceMappingURL=index.d.ts.map