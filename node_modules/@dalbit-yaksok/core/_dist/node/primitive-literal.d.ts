import { BooleanValue, NumberValue, StringValue } from "../value/primitive.js";
import { Evaluable, Node } from "./base.js";
import { YaksokError } from "../error/common.js";
import type { Token } from "../prepare/tokenize/token.js";
import type { Scope } from "../executer/scope.js";
export declare class NumberLiteral extends Evaluable {
  private content: any;
  tokens: Token[];
  static friendlyName: string;
  constructor(content: number, tokens: Token[]);
  override execute(_scope: Scope): Promise<NumberValue>;
  override toPrint(): string;
  toNumber(): number;
  override validate(): YaksokError[];
}
export declare class StringLiteral extends Evaluable {
  private content: any;
  tokens: Token[];
  static friendlyName: string;
  constructor(content: string, tokens: Token[]);
  override execute(_scope: Scope): Promise<StringValue>;
  override toPrint(): string;
  override validate(): YaksokError[];
}
export declare class BooleanLiteral extends Evaluable {
  private content: any;
  tokens: Token[];
  static friendlyName: string;
  constructor(content: boolean, tokens: Token[]);
  override execute(_scope: Scope): Promise<BooleanValue>;
  override toPrint(): string;
  override validate(): YaksokError[];
}
export declare class TemplateStringPart extends Node {
  content: string;
  tokens: Token[];
  static friendlyName: string;
  constructor(content: string, tokens: Token[]);
  override toPrint(): string;
  override validate(): YaksokError[];
}
export declare class TemplateLiteral extends Evaluable {
  parts: (TemplateStringPart | Evaluable)[];
  tokens: Token[];
  static friendlyName: string;
  constructor(parts: (TemplateStringPart | Evaluable)[], tokens: Token[]);
  override execute(scope: Scope): Promise<StringValue>;
  override toPrint(): string;
  override validate(scope: Scope): YaksokError[];
}
//# sourceMappingURL=primitive-literal.d.ts.map