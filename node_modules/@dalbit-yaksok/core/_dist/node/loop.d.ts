import { Executable } from "./base.js";
import { YaksokError } from "../error/common.js";
import type { Scope } from "../executer/scope.js";
import { type Token } from "../prepare/tokenize/token.js";
import type { Block } from "./block.js";
export declare class Loop extends Executable {
  body: Block;
  tokens: Token[];
  static friendlyName: string;
  constructor(body: Block, tokens: Token[]);
  override execute(scope: Scope): Promise<void>;
  override validate(scope: Scope): YaksokError[];
}
export declare class Break extends Executable {
  tokens: Token[];
  static friendlyName: string;
  constructor(tokens: Token[]);
  override execute(_scope: Scope): Promise<never>;
  override validate(): YaksokError[];
}
export declare class Continue extends Executable {
  tokens: Token[];
  static friendlyName: string;
  constructor(tokens: Token[]);
  override execute(_scope: Scope): Promise<never>;
  override validate(): YaksokError[];
}
//# sourceMappingURL=loop.d.ts.map