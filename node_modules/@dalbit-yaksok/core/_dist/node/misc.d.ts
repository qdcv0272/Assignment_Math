import { YaksokError } from "../error/common.js";
import { Evaluable, Executable, Node } from "./base.js";
import type { Scope } from "../executer/scope.js";
import type { Token } from "../prepare/tokenize/token.js";
import { StringValue } from "../value/primitive.js";
export declare class EOL extends Node {
  tokens: Token[];
  static friendlyName: string;
  constructor(tokens: Token[]);
  override validate(): YaksokError[];
}
export declare class Indent extends Node {
  size: number;
  tokens: Token[];
  static friendlyName: string;
  constructor(size: number, tokens: Token[]);
  override validate(): YaksokError[];
}
export declare class Print extends Executable {
  value: Evaluable;
  tokens: Token[];
  static friendlyName: string;
  constructor(value: Evaluable, tokens: Token[]);
  override execute(scope: Scope): Promise<void>;
  override validate(scope: Scope): YaksokError[];
}
export declare class TypeOf extends Evaluable {
  value: Evaluable;
  tokens: Token[];
  static friendlyName: string;
  constructor(value: Evaluable, tokens: Token[]);
  override execute(scope: Scope): Promise<StringValue>;
  override validate(scope: Scope): YaksokError[];
}
export declare class Pause extends Executable {
  tokens: Token[];
  static friendlyName: string;
  constructor(tokens: Token[]);
  override execute(scope: Scope): Promise<void>;
  override validate(_scope: Scope): YaksokError[];
}
//# sourceMappingURL=misc.d.ts.map