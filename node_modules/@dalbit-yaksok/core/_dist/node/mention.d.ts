import { YaksokError } from "../error/common.js";
import { ValueType } from "../value/base.js";
import { Evaluable, Identifier, Node } from "./base.js";
import { FunctionInvoke } from "./function.js";
import type { Scope } from "../executer/scope.js";
import type { Token } from "../prepare/tokenize/token.js";
export declare class Mention extends Node {
  value: string;
  tokens: Token[];
  static friendlyName: string;
  constructor(value: string, tokens: Token[]);
  override toPrint(): string;
  override validate(scope: Scope): YaksokError[];
}
export declare class MentionScope extends Evaluable {
  fileName: string;
  child: FunctionInvoke | Identifier;
  tokens: Token[];
  static friendlyName: string;
  constructor(fileName: string, child: FunctionInvoke | Identifier, tokens: Token[]);
  override execute(scope: Scope): Promise<ValueType>;
  override toPrint(): string;
  override validate(scope: Scope): YaksokError[];
}
//# sourceMappingURL=mention.d.ts.map