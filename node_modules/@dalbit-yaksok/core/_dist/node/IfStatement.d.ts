import { type Evaluable, Executable } from "./base.js";
import type { Block } from "./block.js";
import { YaksokError } from "../error/common.js";
import type { Scope } from "../executer/scope.js";
import type { Token } from "../prepare/tokenize/token.js";
interface Case {
  condition?: Evaluable;
  body: Block;
}
export declare class IfStatement extends Executable {
  cases: Case[];
  tokens: Token[];
  static friendlyName: string;
  constructor(cases: Case[], tokens: Token[]);
  override execute(scope: Scope): Promise<void>;
  shouldStop(condition: Evaluable | undefined, scope: Scope): Promise<boolean>;
  override validate(scope: Scope): YaksokError[];
}
export declare class ElseStatement extends Executable {
  body: Block;
  tokens: Token[];
  static friendlyName: string;
  constructor(body: Block, tokens: Token[]);
  override validate(scope: Scope): YaksokError[];
}
export declare class ElseIfStatement extends Executable {
  elseIfCase: Case;
  tokens: Token[];
  static friendlyName: string;
  constructor(elseIfCase: Case, tokens: Token[]);
  override validate(scope: Scope): YaksokError[];
}
//# sourceMappingURL=IfStatement.d.ts.map